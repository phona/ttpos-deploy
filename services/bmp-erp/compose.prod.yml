# services/bmp-erp/compose.prod.yml
# Production environment overrides for BMP ERP Service
# Compatible with ttpos-server-go production setup

services:
  bmp-erp:
    image: ${IMAGE_REGISTRY:-hub.hitosea.com/ttpos-server}/ttpos-server-erp:${IMAGE_VERSION}
    container_name: ttpos-bmp-erp-${APP_ID:-prod}
    ports: []
    environment:
      - GIN_MODE=release
      - LOG_LEVEL=${LOG_LEVEL:-warn}
      - GRPC_ENDPOINTS=${GRPC_ENDPOINTS}:14022
    volumes:
      - ${CONFIG_PATH:-./app/ttpos-erp/manifest/config}:/app/config:ro
      - ${LOG_PATH:-./docker/bmp-erp/logs}:/app/log
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 1G
        reservations:
          cpus: '250m'
          memory: 256M
      restart_policy:
        condition: on-failure
        delay: 5s
        max_attempts: 3
        window: 120s
    logging:
      driver: json-file
      options:
        max-size: "50m"
        max-file: "10"
    healthcheck:
      test: ["CMD", "grpc_health_probe", "-addr=:14022"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    labels:
      - "com.ttpos.service=bmp-erp"
      - "com.ttpos.version=${IMAGE_VERSION}"
      - "com.ttpos.environment=production"
