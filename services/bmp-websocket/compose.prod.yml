# services/bmp-websocket/compose.prod.yml
# Production environment overrides for BMP WebSocket Service
# Compatible with ttpos-server-go production setup

services:
  bmp-websocket:
    image: ${IMAGE_REGISTRY:-hub.hitosea.com/ttpos-server}/ttpos-server-websocket:${IMAGE_VERSION}
    container_name: ttpos-bmp-websocket-${APP_ID:-prod}
    ports: []
    environment:
      - GIN_MODE=release
      - LOG_LEVEL=${LOG_LEVEL:-warn}
      - GRPC_ENDPOINTS=${GRPC_ENDPOINTS}:14052
    volumes:
      - ${CONFIG_PATH:-./app/ttpos-websocket/manifest/config}:/app/config:ro
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 1G
        reservations:
          cpus: '250m'
          memory: 256M
      restart_policy:
        condition: on-failure
        delay: 5s
        max_attempts: 3
        window: 120s
    logging:
      driver: json-file
      options:
        max-size: "50m"
        max-file: "10"
    healthcheck:
      test: ["CMD", "grpc_health_probe", "-addr=:14052"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    labels:
      - "com.ttpos.service=bmp-websocket"
      - "com.ttpos.version=${IMAGE_VERSION}"
      - "com.ttpos.environment=production"
